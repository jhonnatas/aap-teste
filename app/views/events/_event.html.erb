<div class="card">
  <div class="card-body">
      <div class="card-body d-flex justify-content-between">
          <span> <%= event.name %> </span>
      </div>
  </div>
  <div class="card-footer">
      
          <%= link_to 'Editar', edit_event_path(event), class: 'btn btn-danger' %>
      
  </div>
</div>